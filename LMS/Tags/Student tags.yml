info:
  name: Student tags
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseurl}}/students/2/tags"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
