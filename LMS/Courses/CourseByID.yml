info:
  name: CourseByID
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseurl}}/courses/38"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
