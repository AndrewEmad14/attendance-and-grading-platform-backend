info:
  name: login
  type: http
  seq: 1

http:
  method: POST
  url: "{{baseurl}}/auth/login/"
  body:
    type: json
    data: |-
      {
        "email":"isabelle.haag@example.net",
        "password":"password"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
