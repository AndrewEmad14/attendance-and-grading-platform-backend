info:
  name: Create Course
  type: http
  seq: 2

http:
  method: POST
  url: "{{baseurl}}/cohorts/1/courses"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
