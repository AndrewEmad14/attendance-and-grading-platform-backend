info:
  name: update user
  type: http
  seq: 6

http:
  method: PATCH
  url: "{{baseurl}}/users/:id"
  params:
    - name: id
      value: "373"
      type: path
  body:
    type: json
    data: |-
      {
        "email":"andrew14emad@gmail.com"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
