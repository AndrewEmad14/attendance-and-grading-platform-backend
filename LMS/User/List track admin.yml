info:
  name: List track admin
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseurl}}/users/track-admins"
  params:
    - name: page
      value: "1"
      type: query
      disabled: true
    - name: name
      value: ""
      type: query
      disabled: true
    - name: cohort
      value: "1"
      type: query
      disabled: true
    - name: track_id
      value: "1"
      type: query
      disabled: true
    - name: is_active
      value: "true"
      type: query
      disabled: true
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
