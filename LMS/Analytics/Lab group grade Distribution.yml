info:
  name: Lab group grade Distribution
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseurl}}/analytics/lab-groups/1"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
