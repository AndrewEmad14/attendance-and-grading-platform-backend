info:
  name: GetAtRiskStudents
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseurl}}/analytics/cohorts/1/at-risk"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
