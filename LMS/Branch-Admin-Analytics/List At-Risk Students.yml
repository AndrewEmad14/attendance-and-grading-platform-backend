info:
  name: List At-Risk Students
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseurl}}/analytics/cohorts/1/at-risk"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
