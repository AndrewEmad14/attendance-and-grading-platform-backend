info:
  name: List Branch Wide Analytics
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseurl}}/analytics/branch"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
