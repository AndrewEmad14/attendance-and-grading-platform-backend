info:
  name: List Billing For Specific Staff_id
  type: http

http:
  method: GET
  url: "{{baseurl}}/billing/instructors/{staffProfileId}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
​