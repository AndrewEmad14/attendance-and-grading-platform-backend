info:
  name: List Billing Rollup
  type: http

http:
  method: GET
  url: "{{baseurl}}/billing/rollup"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
​