info:
  name: GetExcuseRequestList
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseurl}}/excuse-requests"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
