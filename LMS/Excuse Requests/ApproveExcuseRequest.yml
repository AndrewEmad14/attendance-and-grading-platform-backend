info:
  name: ApproveExcuseRequest
  type: http
  seq: 6

http:
  method: POST
  url: "{{baseurl}}/excuse-requests/1/approve"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
