info:
  name: GetExcuseRequest
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseurl}}/excuse-requests/1"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
