info:
  name: CreateExcuseRequestNoAttachment
  type: http
  seq: 4

http:
  method: POST
  url: "{{baseurl}}/excuse-requests"
  body:
    type: json
    data: |
      {
        "attendance_id": 1,
        "reason": "Family emergency."
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
