info:
  name: CheckOut
  type: http
  seq: 4

http:
  method: POST
  url: "{{baseurl}}/attendance"
  body:
    type: json
    data: |
      {
        "engagement_id": 1
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
