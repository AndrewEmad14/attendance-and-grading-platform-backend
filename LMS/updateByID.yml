info:
  name: updateByID
  type: http
  seq: 4

http:
  method: PATCH
  url: "{{baseurl}}/courses/3"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
