info:
  name: Delete Submission
  type: http
  seq: 7

http:
  method: DELETE
  url: "{{baseurl}}/submissions/1442"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
