info:
  name: view missing Students
  type: http
  seq: 6

http:
  method: GET
  url: "{{baseurl}}/deliverables/1/missing/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
