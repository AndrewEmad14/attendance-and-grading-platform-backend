info:
  name: Create Submission
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseurl}}/deliverables/1/submissions/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
