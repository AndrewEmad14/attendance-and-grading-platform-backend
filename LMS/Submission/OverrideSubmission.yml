info:
  name: OverrideSubmission
  type: http
  seq: 2

http:
  method: POST
  url: "{{baseurl}}/submissions/1441/override"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
