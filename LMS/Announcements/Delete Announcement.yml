info:
  name: Delete Announcement
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{baseurl}}/announcements/:id"
  params:
    - name: id
      value: "31"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
