info:
  name: Get Announcements
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseurl}}/cohorts/:id/announcements"
  params:
    - name: id
      value: "1"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
